export const CSS_COLORS_PREFIX = '--colors';

export const CSS_CONSTANT_PREFIX = '-';

export const PREFERS_LOCAL_KEY = 'data-color-mode'

export const PREFERS_THEME_KEY = 'data-theme'

export const PREFERS_THEME_CSS_PROP = `--color-scheme`
